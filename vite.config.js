import { defineConfig } from 'vite'

export default defineConfig({
  base: '/japanese-Budoux/', // Change 'your-repo' to your actual GitHub repo name
  build: {
    outDir: 'dist', // This is the folder where build files will go
  },
})
